link-https://practice.geeksforgeeks.org/problems/three-way-partitioning/1#
code
int k=0,n=A.size();
    int l=n-1;
    for(int i=0;i<=l;i++)
    {
        if(A[i]<a)
            swap(A[i],A[k++]);
        else if(A[i]>b)
            swap(A[i--],A[l--]);
    }
