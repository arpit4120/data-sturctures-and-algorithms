//https://practice.geeksforgeeks.org/problems/minimum-element-in-bst/1
int minValue(Node* root)
{
    // Code here
    int y;
    if(root==NULL)
        return -1;
    if(root->left!=NULL)
        y=minValue(root->left);
    else
        y=root->data;
    return y;
        
}
